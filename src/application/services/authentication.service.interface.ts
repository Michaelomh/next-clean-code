// Contains interface for authentication and authorization services (3rd party services)

// export interface IAuthenticationService {
// generateUserId(): string;
// validateSession(
//   sessionId: Session['id']
// ): Promise<{ user: User; session: Session }>;
// validatePasswords(
//   inputPassword: string,
//   usersHashedPassword: string
// ): Promise<boolean>;
// createSession(user: User): Promise<{ session: Session; cookie: Cookie }>;
// invalidateSession(sessionId: Session['id']): Promise<{ blankCookie: Cookie }>;
// }

export {}
